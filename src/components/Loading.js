import React from 'react';

import './loading.css';

const Loading = () => {
    return (
        <span className="RE-LOADING">
            <span />
        </span>
    );
};

export default Loading;
